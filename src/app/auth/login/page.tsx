import LoginForm from './LoginForm'

export default function LoginPage() {
  // Just render the login form
  // Session redirect is handled client-side in LoginForm after successful login
  return <LoginForm />
}
